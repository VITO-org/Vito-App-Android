<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- React Native requiere INTERNET para Metro bundler en debug -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permisos para Android 14+ (Integrado en el sistema) -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE" />

    <!-- Permisos para Android 13 e inferiores (App Health Connect de la Play Store) -->
    <uses-permission android:name="androidx.health.permission.Steps.READ" />
    <uses-permission android:name="androidx.health.permission.Distance.READ" />
    <uses-permission android:name="androidx.health.permission.HeartRate.READ" />
    <uses-permission android:name="androidx.health.permission.Sleep.READ" />
    <uses-permission android:name="androidx.health.permission.Exercise.READ" />
    <uses-permission android:name="androidx.health.permission.TotalCaloriesBurned.READ" />
    <uses-permission android:name="androidx.health.permission.BloodPressure.READ" />
    <uses-permission android:name="androidx.health.permission.OxygenSaturation.READ" />
    <uses-permission android:name="androidx.health.permission.BodyTemperature.READ" />

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Obligatorio para Health Connect: pantalla de rationale de permisos -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Deep link para OAuth callback de Supabase -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.vito.healthconnect.rn"
                    android:host="auth/callback" />
            </intent-filter>
        </activity>

        <!-- Requerido para Health Connect en versiones modernas -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

    </application>
</manifest>
