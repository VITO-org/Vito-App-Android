# Cortex configuration -- auto-generated by cortex setup
# Project: vito-app-android (java)

episodic:
  persist_dir: memory
  collection_name: cortex_episodic
  embedding_model: all-MiniLM-L6-v2
  embedding_backend: onnx  # onnx | local | openai
  namespace_mode: project  # project | branch | custom
  namespace_value: ""

semantic:
  vault_path: vault

retrieval:
  top_k: 5
  episodic_weight: 1.0
  semantic_weight: 1.0

llm:
  provider: none
  model: ""

integrations:
  jira:
    enabled: true
    base_url: "https://vitoproyecto-1775921969007.atlassian.net"
    email_env: JIRA_EMAIL
    token_env: JIRA_API_TOKEN
